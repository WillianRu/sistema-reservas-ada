server:
  port: 8080

spring:
  application:
    name: cinema-db
  datasource:
    password: willianbdb
    url: jdbc:postgresql://localhost:5432/cinema-db
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

